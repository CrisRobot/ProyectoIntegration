//import { agregar, eliminar } from "./actions";

//const initialState = {
  //  myFavorites: []
//};

//const reducer = (state = initialState, action) => {
  //  switch(action.type){
    //    case AGREGAR : return{
      //      ...state, 
        //    myFavorites: [...state.myFavorites, action.payload]
        //}

        //case ELIMINAR : return{
          //  ...state,
            //myFavorites: [... state.myFavorites.filter(param => param.id !== action.payload)]
        //}
        //default: return{...state};
    //}
    
//};

//export default reducer;